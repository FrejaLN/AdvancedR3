# usethis as a workflow dependency when you work in the console
# workflow dependency: used in the console or in the palette and not in the script

#Style all files in a project
styler::style_dir()

#Targets is a build dependency as we will have a target file both will be used a lot in the console

#Tidyverse is a set of packages, and can therefore not be stored as a dependency, but we can load it specific

#It runs the setup code chunk each time you restart

#to add a package to the description file: use_package("")
